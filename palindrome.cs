using System;
namespace Assignment2
{
    class Palindrome
    {
       /* static void Main(string[] args)
        {
            Console.WriteLine("Enter any  palindrome number ");
             num = int.Parse(Console.ReadLine());
            int rem;
            int sum = 0;
            int temp = num;
            while (num > 0)
            {
                rem = num % 10;
                sum = (sum * 10) + rem;
                num = num / 10;
            }
            if (temp == sum)
            {
                Console.WriteLine($"{temp} is a Palindrome");
            }
        }*/
    }
}

